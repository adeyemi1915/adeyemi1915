function calculateModecafe (speed) {
    const speedlimit=80;
    const KmPerModecafe=5;
    const maxModecafe=20;

    if (speed<speedLimit){
        console. log ("ok");
        return;
    }

} 
    const Modecafe = Math. floor ( (speed-speedLimit) /KmPerModecafe);{

    if(modecafe<maxModecafe){
            console.log("license suspended");
    }else{
            console.log(modecafe);
    }

}
